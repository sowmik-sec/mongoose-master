import { TEnrolledCourse } from './enrolledCourse.interface';

const createEnrolledCourseIntoDB = (payload: TEnrolledCourse) => {};

export const EnrolledCourseService = {
  createEnrolledCourseIntoDB,
};
